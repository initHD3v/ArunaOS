{
  "name": "@arunaos/runtime",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .ts,.tsx",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf node_modules dist"
  },
  "devDependencies": {
    "@arunaos/types": "workspace:*",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^5.8.0",
    "vitest": "^4.1.10"
  }
}
